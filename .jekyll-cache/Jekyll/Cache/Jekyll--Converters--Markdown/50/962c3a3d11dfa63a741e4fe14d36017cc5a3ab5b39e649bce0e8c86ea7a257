I"�<p><strong>map</strong>은 <strong>list</strong>의 요소를 <strong>지정된 함수</strong>로 처리하는 함수이다. map은 원본 리스트를 변경하지 않고 새 리스트를 생성한다.<br /><br /><br /></p>

<blockquote>
  <p>map을 사용하지 않은 예(대화형 Python)</p>
</blockquote>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><table class="rouge-table"><tbody><tr><td class="rouge-gutter gl"><pre class="lineno">1
2
3
4
5
6
</pre></td><td class="rouge-code"><pre><span class="o">&gt;&gt;&gt;</span> <span class="n">a</span> <span class="o">=</span> <span class="p">[</span><span class="mf">1.5</span><span class="p">,</span> <span class="mf">3.5</span><span class="p">,</span> <span class="mf">2.7</span><span class="p">]</span>
<span class="o">&gt;&gt;&gt;</span> <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="nb">len</span><span class="p">(</span><span class="n">a</span><span class="p">))</span>
<span class="o">...</span>		<span class="n">a</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="nb">int</span><span class="p">(</span><span class="n">a</span><span class="p">[</span><span class="n">i</span><span class="p">])</span>
<span class="o">...</span>
<span class="o">&gt;&gt;&gt;</span> <span class="n">a</span>
<span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">2</span><span class="p">]</span>
</pre></td></tr></tbody></table></code></pre></div></div>

<p>이런 식으로 <strong>C/C++</strong> 처럼 반복문으로 요소마다 모두 접근을 해야 한다.<br /></p>

<p>하지만 map을 사용하면 간편하게 list의 모든요소에 간결하게 접근할 수 있다.<br /><br /></p>

<p><br /></p>

<blockquote>
  <p>map을 사용한 예</p>
</blockquote>

<pre><code class="language-Python">&gt;&gt;&gt; a = [1.5, 3.5, 2.7]
&gt;&gt;&gt; a = list(map(int, a))
&gt;&gt;&gt; a
[1, 3, 2]
</code></pre>

<p><br /><br /></p>

<h3 id="map의-범용성자료형"><strong>map의 범용성(자료형)</strong></h3>

<p>map에는 리스트 뿐아니라 모든 <code class="highlighter-rouge">반복가능한 객체</code>를 넣을 수 있다.</p>

<pre><code class="language-Python">&gt;&gt;&gt; a = list(map(str, range(5)))
&gt;&gt;&gt; a
['0', '1', '2', '3', '4']
</code></pre>

<h2 id="inputsplit과-map"><strong>Input().split()과 map</strong></h2>

<p><code class="highlighter-rouge">input().split()</code>으로 여러 개의 값을 입력받고 정수, 실수로 변환할 때도 map을 사용했었는데, <code class="highlighter-rouge">input().split()</code> 의 결과가 문자열 리스트여서 map을 사용할 수 있었다.<br /></p>

<p><br /></p>

<blockquote>
  <p>input().split()으로 10 20을 입력받은 경우</p>
</blockquote>

<pre><code class="language-Python">&gt;&gt;&gt; a = input().split()
10 20
&gt;&gt;&gt; a
['10', '20']
</code></pre>

<p><br /></p>

<p>10 20을 입력하면 <code class="highlighter-rouge">['10', '20']</code> 처럼 문자열 2개가 들어가있는 리스트가 만들어진다.<br /></p>

<p><br /></p>

<blockquote>
  <p>input().split()으로 10 20을 입력받고 map을 통해 int로 변환</p>
</blockquote>

<pre><code class="language-Python">&gt;&gt;&gt; a = map(int, input().split())
10 20
&gt;&gt;&gt; a
&lt;map object at 0x03DFB0D0&gt;
&gt;&gt;&gt; list(a)
[10, 20]
</code></pre>

<p><code class="highlighter-rouge">list(a)</code>를 통해 list안에 들어가있는 int형 정수 2개를 뽑을 수 있었다.</p>

<hr />

<p>Ref.  https://dojang.io/mod/page/view.php?id=2286</p>
:ET